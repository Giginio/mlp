## Documentation

install libraries with mlpenv.yml

run form code directory:
conda env create -f tf_env.yml


The setup of a MySQL server and MySQL Workbench are needed to import of the sql database. The import of the database import_MySQL.sql is explained in the pdf Import_MySQL_db

Additionally the chromedriver has to be installed and referenced in the python notebook.